language: elixir

otp_release:
  - 19.1

elixir:
  - 1.3.0
  - 1.3.3
  - 1.4.5
  - 1.5.0-rc.0

sudo: false

script:
  - MIX_ENV=test mix do compile --warnings-as-errors, coveralls.json

after_success:
  - bash <(curl -s https://codecov.io/bash)

